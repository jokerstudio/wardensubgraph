{
  "name": "Uderward",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ Uderward",
    "create-local": "graph create --node http://localhost:8020/ Uderward",
    "remove-local": "graph remove --node http://localhost:8020/ Uderward",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Uderward"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
